kind: Sync
apiVersion: nomos.dev/v1alpha1
metadata:
  name: pvc-sync
spec:
  groups:
    - group: ""
      kinds:
      - kind: PersistentVolumeClaim
        hierarchyMode: inherit
        versions:
          - version: v1
            
